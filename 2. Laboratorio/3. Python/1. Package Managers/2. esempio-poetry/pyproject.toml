[project]
name = "2-esempio-poetry"
version = "0.1.0"
description = ""
authors = [
    {name = "Alessandro Sanino",email = "3513298+saniales@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests (>=2.32.5,<3.0.0)"
]

[tool.poetry]
packages = [{include = "2_esempio_poetry", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
